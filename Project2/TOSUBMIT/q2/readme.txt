"At least one" constraints in relationships on the ER diagram cannot be enforced in the database implementation.
We should have used serialized for most of our primary keys rather than int but we discovered that property too late to implement it well in the already generated data tables.
